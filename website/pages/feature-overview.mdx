---
description: A tour of everything governed model access gives you today.
---

import { Callout } from 'nextra/components'
import { Figure } from '../components'

# Feature overview

What the SDK gives you **today**, wired to the live-verified proxy contract.
For what is designed but not yet shipped, see the [Roadmap](/roadmap) — nothing
on this page is speculative.

<Figure
  src="/img/sdk-architecture.svg"
  alt="Agent Fabric SDK architecture: your unchanged framework code calls the SDK, which resolves config, builds the native object, governs the transport, and classifies responses into typed errors; the call goes out as one OpenAI-compatible HTTPS request with client_id, client_secret, correlation and attribution headers injected, through the Omni Gateway LLM proxy — which enforces consumer auth, PII detection, token budget and metering — and on to upstream model providers."
  caption="The whole path, end to end. The SDK occupies one layer: it injects governance into the transport, and the gateway — not your process — enforces policy."
  width={1800}
  height={1160}
/>

Everything the SDK adds attaches at stage 2 of that diagram, on a single shared
transport. That is the **skeleton**: one place where every request enters and
every response leaves, which is why budget, spans, correlation IDs and
simulation can all be added later without you rewiring a call site.

## Native framework objects, not wrappers

One call per framework returns the framework's **own** object, already pointed at
the proxy with the verified `client_id`/`client_secret` headers, attribution, and
retry policy injected:

| Framework | Call | Returns |
|---|---|---|
| LangGraph | `fabric.langgraph.chat_model("gpt-4o")` | `langchain_openai.ChatOpenAI` |
| Google ADK | `fabric.adk.model("gpt-4o")` | `google.adk … LiteLlm` |
| Strands | `fabric.strands.model("gpt-4o")` | `strands … OpenAIModel` |
| MS Agent Framework | `fabric.agent_framework.chat_client("gpt-4o")` | Agent Framework chat client |
| LlamaIndex | `fabric.llamaindex.llm("gpt-4o")` | `OpenAILike` (`is_chat_model=True`) |
| OpenAI Agents SDK | `fabric.openai.model("gpt-4o")` | `agents.OpenAIChatCompletionsModel` |
| Anthropic SDK | `fabric.anthropic.client()` | `anthropic.AsyncAnthropic` (client, not model-bound) |
| CrewAI | `fabric.crewai.llm("gpt-4o")` | `crewai.LLM` (LiteLLM-backed) |

See [Model access](/frameworks) for each framework's page. Support **depth**
differs even though the shape does not: LangGraph is the one deep,
conformance-gated adapter, and the other seven are supported at the
`connection_kwargs()` level.

## Three ways to construct — pick your ergonomics

Every adapter offers the same governed connection three ways, from one source of
truth:

```python
# 1. Factory method — shared client + explicit lifecycle
async with Fabric.from_env() as fabric:
    model = fabric.langgraph.chat_model("gpt-4o", temperature=0.2)

# 2. Module-level factory — shortest; cached default from the environment
from agent_fabric.integrations.langgraph import chat_model
model = chat_model("gpt-4o", temperature=0.2)

# 3. Governed-kwargs accessor — you build the native object yourself
from langchain_openai import ChatOpenAI
model = ChatOpenAI(model="gpt-4o", **fabric.langgraph.connection_kwargs())
```

Prefer the factory when you want shared-client reuse and lifecycle
(`async with`); reach for the accessor when you want full control of the
constructor.

## Framework-free client

`fabric.llm.client()` returns a native `AsyncOpenAI` aimed at the proxy — use the
OpenAI SDK exactly as you normally would (Chat Completions or the Responses API,
streaming included); the SDK adds the governance headers and retry policy.

Pass `sync=True` for the blocking `OpenAI` instead, governed on identical terms —
same base URL, same verified `client_id` / `client_secret` headers, same
correlation ID and retry policy:

```python
with Fabric.from_env() as fabric:
    client = fabric.llm.client(sync=True)      # openai.OpenAI
    reply = client.chat.completions.create(
        model="gpt-4o",
        messages=[{"role": "user", "content": "Say hi in three words."}],
    )
    print(reply.choices[0].message.content)
```

The two forms are declared as typed overloads, so your editor narrows the result
to `OpenAI` or `AsyncOpenAI` and keeps completing on it. Note the boundary: the
blocking transport takes no `AuthProvider`, because that protocol is async-only.
That costs nothing here — the proxy authenticates on the header pair, not a
fetched token — but it does mean the control-plane surfaces (`registry`,
`tools`) remain async.

## A typed governed error taxonomy

`classify()` maps the proxy's four live rejection shapes to typed exceptions —
`AuthError` (401), `PIIDetected` (403), `TokenBudgetExceeded` (429), plus upstream
passthrough — so you branch on governance outcomes instead of parsing bodies.
See [Error taxonomy](/errors).

Those exception types are the client-side mirror of the policies the gateway
enforces. The **LLM** lane below is the one this SDK targets today; the same
control plane applies equivalent policy sets to APIs, MCP tools, and A2A
agents. Those two columns are not decoration — **MCP** is what
[tool access](/tool-access) reaches in Phase 2, and **Agents** is what
[A2A](/a2a) reaches in the same phase.

<Figure
  src="/img/omni-gateway-governance-layer.jpg"
  alt="A unified governance layer across four asset types. APIs enforce OAuth 2.0/JWT validation, client-ID enforcement, rate limiting and SLA tiers, IP allowlisting, and tokenization. LLMs enforce token-based rate limits, semantic prompt guard, content moderation, PII detection and masking, and prompt decorators. MCP enforces global access control, attribute-based access control, payload optimization, PII detection, and tool mapping. Agents enforce token-based rate limits, PII detection, prompt decorators, in-task authorization codes, and OAuth 2.0 on-behalf-of credential injection."
  caption="The policy sets behind the error taxonomy. Token rate limits and PII detection in the LLM column are the two the SDK already maps to TokenBudgetExceeded and PIIDetected."
  width={1024}
  height={575}
/>

## Model handles without a `/models` endpoint

The governed proxy has **no** catalog endpoint (`GET /models` → `404`, verified).
`fabric.llm.resolve("gpt-4o")` gives you a heuristic capability handle
(function-calling / vision / json-output) from a known id, and
`list_models(live=True)` raises a clear `ConfigError` explaining the absence
rather than fabricating a path.

<Callout type="warning">
  **Honesty note (§0.3).** The proxy *contract* the adapters target is
  live-verified, but the exact framework *constructor signatures* are still being
  confirmed against installed versions. Where a
  name can't be resolved, the adapter raises `blocked on verification` rather
  than guess. The [Verification policy](/concepts/verification) page tracks
  current status.
</Callout>
